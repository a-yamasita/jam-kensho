import React from 'react';
import { Card as _Card } from '@mui/material';
/*
import {
    as _
} from '@mui/material';

export default function _(props) {
    return (<_ {...props}>{props.children}</_>);
}
*/

export default function Card(props) {
    return (<_Card {...props}>{props.children}</_Card>);
}